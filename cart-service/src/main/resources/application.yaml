server:
  port: 8089

spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:mysql://localhost:3306/smartfood
    username: root
    password: Charanraj@02

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka